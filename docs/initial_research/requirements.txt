# --- audio capture (all platforms) ---
soundcard>=0.4.3   ; platform_system != "Linux"   # Linux uses PipeWire directly
numpy>=1.26         # only needed by the non-PipeWire backend + local whisper

# --- silence gating: big cost saver, optional but recommended ---
webrtcvad-wheels>=2.0.14 ; platform_system != "Darwin"
webrtcvad>=2.0.10 ; platform_system == "Darwin"

# --- engine: google (pick what you need) ---
google-cloud-speech>=2.27.0

# --- engine: deepgram ---
websockets>=13.0

# --- engine: local whisper ---
# faster-whisper>=1.1.0
